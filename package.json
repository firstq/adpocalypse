{
  "name": "adpocalypse",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:itch": "tsc && vite build && cd dist && zip -r ../adpocalypse-itch.zip .",
    "build:yandex": "tsc && vite build && cd dist && zip -r ../adpocalypse-yandex.zip .",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
